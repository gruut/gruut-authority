image: node:10.12.0

cache:
  paths:
  - node_modules/

stages:
  - build
  - test
  - deploy

setup:
  stage: build
  script:
    - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client -y )'
    - npm install

#mysql_test:
#  stage: test
#  services:
#    - mariadb:10.1.34
#  variables:
#    MYSQL_DATABASE: gruut_authority_test
#    MYSQL_ROOT_PASSWORD: mysql
#  before_script:
#    - npm install -g sequelize-cli
#    - sequelize db:create --env test
#    - sequelize db:migrate --env test
#  script:
#    - npm test

lint:
  stage: test
  script:
    - npm install -g eslint
    - npm run lint

deploy:
  only:
    - master
  stage: deploy
  script:
    - bash deploy/deploy.sh